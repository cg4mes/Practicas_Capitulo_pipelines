name: suma de dos variables de entorno

on:
  push:
    branches: 
      - test/**

jobs:
  suma_variables:
    runs-on: ubuntu-latest
    steps:
    - name: suma de variables de entorno
      run: |
        echo "Hola mundo"