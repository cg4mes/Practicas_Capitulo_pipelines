name: suma de variables de entorno

on:
  push:
    branches: 
      - 'test*'
      
jobs:
  suma:
    name: suma de variables
    runs-on: ubuntu-latest
    outputs: 
      resultado: ${{ steps.suma.outputs.resultado }}
    steps:
    - name: Operar las variables de entorno
      id: suma
      run: |
        resultado=$((${{ vars.A }} + ${{ vars.B }}))
        echo "resultado=$resultado" >> $GITHUB_OUTPUT

  validar_resultado:
    name: Validación del resultado
    runs-on: ubuntu-latest
    needs: suma
    steps:
    - name: Comparar resultado
      run: |
        if [ ${{ needs.suma.outputs.resultado }} -gt 10 ]; then
          echo "El resultado de la suma es mayor que 10"
        else
          echo "El resultado de la suma es menor o igual a 10"
        fi
